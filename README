MSP430G2_peripheral
===================

to build a MSP430G2(or above) line's lib as eaiser as possible


auto config & make tool: build.bat
    it generates file "sources.mak" & "depend.mak" which describe all source files and it's dependency recpectively. and the makefile read them as the compilation parameter. 

To generate hex File: all .c source describe inside "sourcesList.txt"
    1. build clean (Enter)
    2. build       (Enter)
    
    note: 
	source list for makefile     : sources.mak
	dependency list for make file: depend.mak
	requirement                  : make、sed、grep、tr、mspgcc

